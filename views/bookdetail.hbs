<div class="container">
    <div class="row">
        <div class="col-4 d-flex justify-content-center">
            <img class="img-fluid img-thumbnail rounded" src="{{ book.image_url }}">
            <p>
                <code>Pages:</code><i>{{book.pages}}</i>
            </p>
            <p>
                <code>Rating: </code> <i>{{ book.rating }}</i>
            </p>
            <p>
                <code>Genre:</code><i>{{book.genres}}</i>
            </p>
        </div>
        <div class="col">
            <h3>{{ book.title }}</h3>
            <p>by {{book.authors}}</p>
            <p>
                {{ book.description }}
            </p>
            <p>
                <a href="/review/{{book.title}}">Find Review</a>
            </p>
{{!--     		<div class="d-flex justify-content-between">
                <a class="btn btn-sm btn-warning" href="/">
                    <i class="fas fa-home mr-1"></i>
                    Back
                </a>
                {{#if hasBook}}
                    <a class="btn btn-sm btn-primary" href="/review" target="_blank">
                    <i class="fas fa-paper-plane mr-1"></i>
                        Official Site
                    </a>
                {{/if}}
            </div> --}}
        </div>
    </div>
</div>